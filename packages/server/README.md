# @ebbjs/server

> **TS package — e2e test harness only.** This is not a runtime. It
> spawns the `ebb_server` Elixir release as a child process and exposes
> a `seed()` helper for E2E tests.
>
> See [`packages/server/docs/`](./docs/) for the contributor docs
> (harness, seed-client, integration tests).
