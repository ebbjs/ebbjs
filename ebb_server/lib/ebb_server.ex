defmodule EbbServer do
  @moduledoc """
  EbbServer is a sync server for collaborative applications.

  It stores Actions in RocksDB, materializes entities into SQLite,
  and serves them over HTTP.
  """
end
